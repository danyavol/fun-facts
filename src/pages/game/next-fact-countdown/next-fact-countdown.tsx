import { Game, Player } from '../../../services/game.service.ts';

type NextFactCountdownProps = {
    game: Game;
    players: Player[];
    me: Player;
};

export function NextFactCountdown(props: NextFactCountdownProps) {
    return null;
}
